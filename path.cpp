#include <bits/stdc++.h>
using namespace std;

int main() {
    int check = 0;
    int m, n; cin >> m >> n;
    vector<int> adj[100];
    for (int i = 0; i < n + 1; i++) {
        int x,y; cin >> x >> y;
        if () {
            
        } else {
            adj[x].push_back(y);
            adj[y].push_back(x);
        }
        
    }
    
}


